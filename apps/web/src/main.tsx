import { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { GoogleLogin, GoogleOAuthProvider } from '@react-oauth/google';
import { AnimatePresence, motion } from 'framer-motion';
import { ArrowRight, CheckCircle2, ChevronRight, CircleDollarSign, CreditCard, HeartHandshake, Home, Landmark, LockKeyhole, Menu, Plus, ShieldCheck, Sparkles, Target, TrendingDown, Wallet, X } from 'lucide-react';
import './styles.css'; import './refresh.css'; import './auth.css';
import './motion.css';
import './features.css';
import './family.css';
import './vault.css';
import './editor.css';
import './loan-details.css';
import './loan-editor.css';
import './loan-editor-polish.css';
import './live-repayment.css';
import './outstanding.css';
import './full-schedule.css';
import './cash-flow.css';
import './feature-pages.css';
import './document-picker.css';
import './review-live.css';
import './cascade-timeline.css';
import './production.css';

type User={id:string;name:string;email:string;avatarUrl?:string|null};
type Loan={name:string;balance:number;emi:number;paid:number;total:number;color:string;icon:'home'|'card'|'car'|'jewel'};
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
const loans:Loan[]=[{name:'Home loan',balance:2840000,emi:29460,paid:38,total:180,color:'#ff7185',icon:'home'},{name:'Vehicle loan',balance:411200,emi:12500,paid:19,total:60,color:'#8368ed',icon:'car'},{name:'Credit card loan',balance:126800,emi:9450,paid:7,total:24,color:'#2fc7a6',icon:'card'},{name:'Jewel loan',balance:180000,emi:0,paid:0,total:12,color:'#e9ad42',icon:'jewel'}];
const Icon=({type}:{type:Loan['icon']})=>type==='home'?<Home/>:type==='car'?<Landmark/>:type==='card'?<CreditCard/>:<CircleDollarSign/>;
const API_URL=(import.meta.env.VITE_API_URL||'http://localhost:4000').replace(/\/+$/,'');
const apiFetch=(path:string,init:RequestInit={})=>{const token=localStorage.getItem('nivara_token');const headers=new Headers(init.headers);if(token)headers.set('Authorization',`Bearer ${token}`);const url=/^https?:\/\//.test(path)?path:`${API_URL}${path}`;return globalThis.fetch(url,{...init,headers})};
const api=async(path:string,body:unknown)=>{const r=await apiFetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.message||'Unable to continue.');return data};
function App(){const [user,setUser]=useState<User|null>(()=>JSON.parse(localStorage.getItem('nivara_user')||'null'));const [auth,setAuth]=useState<'login'|'signup'|null>(null);const done=(data:{user:User;token:string})=>{localStorage.setItem('nivara_user',JSON.stringify(data.user));localStorage.setItem('nivara_token',data.token);setUser(data.user);setAuth(null)};return <GoogleOAuthProvider clientId={import.meta.env.VITE_GOOGLE_CLIENT_ID||'YOUR_GOOGLE_CLIENT_ID'}>{user?<DashboardV3 user={user} logout={()=>{localStorage.clear();setUser(null)}}/>:<><Landing open={setAuth}/><AnimatePresence>{auth&&<Auth mode={auth} close={()=>setAuth(null)} done={done} switchMode={()=>setAuth(auth==='login'?'signup':'login')}/>}</AnimatePresence></>}</GoogleOAuthProvider>}
function Landing({open}:{open:(m:'login'|'signup')=>void}){return <div className="landing"><nav className="landing-nav"><div className="logo"><img src="/nivara-mark.png"/>Nivara</div><div className="nav-links"><a href="#how">How it works</a><a href="#features">Features</a><button className="text-btn" onClick={()=>open('login')}>Log in</button><button className="nav-cta" onClick={()=>open('signup')}>Start free <ArrowRight size={16}/></button></div></nav><main><section className="landing-hero"><div className="hero-copy-land"><div className="trust"><Sparkles size={14}/> YOUR DEBT-FREEDOM COMPANION</div><h1>Money peace,<br/><em>made visible.</em></h1><p>Bring every loan into one reassuring plan. Nivara shows your fastest path to debt-free—with clarity, encouragement, and no judgment.</p><div className="hero-actions"><button className="primary-cta" onClick={()=>open('signup')}>Create my free plan <ArrowRight size={17}/></button><button className="play-cta" onClick={()=>document.getElementById('how')?.scrollIntoView({behavior:'smooth'})}>See how it works <span>→</span></button></div><div className="tiny-proof"><span>✓ Private by design</span><span>✓ Takes 2 minutes</span><span>✓ Always yours</span></div></div><div className="hero-visual"><div className="float-note note-a">✦ <span>₹4.2L interest saved</span></div><div className="float-note note-b">✓ <span>On track this month</span></div><div className="freedom-card"><div className="ring"><div><small>LOAN-FREE IN</small><b>5y 2m</b><span>Sep 2031</span></div></div><p>Your freedom date</p><h3>Every payment counts.</h3><div className="mini-bars"><i/><i/><i/><i/><i/><i/></div></div></div></section><section className="proof-strip"><div><b>64</b><span>payments already<br/>made with confidence</span></div><div><b>₹4.28L</b><span>potential interest<br/>you can avoid</span></div><div><b>One</b><span>calm, actionable<br/>financial plan</span></div></section><section id="how" className="how"><div><p className="kicker">A GENTLER WAY FORWARD</p><h2>Less financial noise.<br/><em>More forward motion.</em></h2></div><div className="steps">{[[<Wallet/>,'Bring it together','Add loans, EMIs and renewals in one private place.'],[<Target/>,'See your best move','Get a payoff order tailored to your interest and balances.'],[<HeartHandshake/>,'Build momentum','Track wins, redirect freed EMIs, and stay encouraged.']].map((s,i)=><article><div className="step-number">0{i+1}</div><div className="step-icon">{s[0]}</div><h3>{s[1]}</h3><p>{s[2]}</p></article>)}</div></section><section id="features" className="feature-callout"><div><p className="kicker">DESIGNED FOR REAL LIFE</p><h2>A plan that flexes<br/>when life does.</h2><p>Got a bonus? Pay an extra EMI? Nivara instantly shows what changes and where your money makes the biggest difference.</p><button className="primary-cta" onClick={()=>open('signup')}>Build my payoff plan <ArrowRight size={17}/></button></div><div className="feature-card"><div><TrendingDown/><span>Smart payoff plan</span></div><b>14 months</b><p>sooner to your freedom date</p><hr/><small>Redirecting one freed EMI can make a bigger difference than you think.</small></div></section></main><footer>© 2026 Nivara <span>Built for your peace of mind.</span></footer></div>}
function Auth({mode,close,done,switchMode}:{mode:'login'|'signup';close:()=>void;done:(x:{user:User;token:string})=>void;switchMode:()=>void}){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{done(await api(mode==='signup'?'/api/auth/signup':'/api/auth/login',mode==='signup'?{name,email,password}:{email,password}))}catch(err){setError(err instanceof Error?err.message:'Could not continue.')}finally{setLoading(false)}};const google=async(credential:string)=>{setLoading(true);setError('');try{done(await api('/api/auth/google',{credential}))}catch(err){setError(err instanceof Error?err.message:'Google sign-in failed.')}finally{setLoading(false)}};return <motion.div className="auth-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="auth-modal" initial={{y:30,opacity:0}} animate={{y:0,opacity:1}} exit={{y:20,opacity:0}}><button className="auth-close" onClick={close}><X/></button><div className="auth-mark"><img src="/nivara-mark.png"/></div><p className="kicker">WELCOME TO NIVARA</p><h2>{mode==='signup'?'Your calmer money chapter starts here.':'Welcome back.'}</h2><p className="auth-sub">{mode==='signup'?'Create your account and make a plan that feels possible.':'Your next best financial move is waiting.'}</p><form onSubmit={submit}>{mode==='signup'&&<label>Your name<input required minLength={2} value={name} onChange={e=>setName(e.target.value)} placeholder="Prashanth Raja"/></label>}<label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label>{error&&<p className="auth-error">{error}</p>}<button disabled={loading} className="auth-submit">{loading?'Please wait…':mode==='signup'?'Create my account':'Log in'} <ArrowRight size={16}/></button></form><div className="divider"><span>or continue with</span></div><div className="google-wrap"><GoogleLogin onSuccess={r=>r.credential&&google(r.credential)} onError={()=>setError('Google sign-in was cancelled.')} shape="rectangular" width="310" text={mode==='signup'?'signup_with':'signin_with'}/></div><p className="switch-auth">{mode==='signup'?'Already have an account?':'New to Nivara?'} <button onClick={switchMode}>{mode==='signup'?'Log in':'Create an account'}</button></p><div className="security-note"><ShieldCheck size={15}/> Your financial data is private and protected.</div></motion.div></motion.div>}
function Dashboard({user,logout}:{user:User;logout:()=>void}){const [page,setPage]=useState('Overview');const nav=[['Overview',<Target/>],['Calendar',<CircleDollarSign/>],['Calculator',<TrendingDown/>],['Strategies',<Sparkles/>],['Cash flow',<Wallet/>],['Cascade',<Landmark/>],['Safeguards',<ShieldCheck/>],['Stress score',<HeartHandshake/>],['Review',<CheckCircle2/>],['Documents',<LockKeyhole/>],['Export',<ArrowRight/>],['Family',<HeartHandshake/>]];return <div className="app-shell"><aside><div className="brand"><img src="/nivara-mark.png"/><span>Nivara</span></div><div className="nav feature-nav">{nav.map(([n,i])=><button className={page===n?'active':''} onClick={()=>setPage(n as string)}>{i}{n}</button>)}</div><div className="side-bottom"><div className="profile"><div>{user.name.slice(0,2).toUpperCase()}</div><span>{user.name}<br/><small>New plan</small></span><button onClick={logout}><ChevronRight size={16}/></button></div></div></aside><main><header><button className="mobile-menu"><Menu/></button><div><p className="eyebrow">NIVARA FINANCIAL COMMAND CENTER</p><h1>{page==='Overview'?`Good morning, ${user.name.split(' ')[0]}`:page} <span>✦</span></h1><p className="sub">Start with your real numbers—nothing pre-filled.</p></div><button className="add-btn"><Plus size={18}/> Add a loan</button></header>{page==='Cash flow'?<CashEditor/>:page==='Calendar'?<CalendarLive/>:<CleanHub page={page}/>}</main></div>}
function Hero({page}:{page:string}){if(page==='My loans')return <section className="card loans dashboard-section"><p className="kicker">ALL COMMITMENTS</p><h2>Every loan, <em>one clear view.</em></h2><LoanRows/></section>;if(page==='Payoff plan')return <section className="payoff-dashboard"><div><p className="kicker">YOUR OPTIMIZED STRATEGY</p><h2>Close the expensive debt <em>first.</em></h2><p>Direct every extra payment to your credit card loan, then roll that EMI into the vehicle loan.</p><button className="primary-cta">Explore scenarios <ArrowRight size={16}/></button></div><div className="payoff-highlight"><small>YOU COULD BE DEBT-FREE</small><b>14 months sooner</b><span>by following your Nivara plan</span></div></section>;if(page==='Timeline')return <section className="card loans dashboard-section"><p className="kicker">PAYMENT CALENDAR</p><h2>Your path, <em>month by month.</em></h2>{[['05 Aug','Home loan EMI','₹29,460'],['08 Aug','Vehicle loan EMI','₹12,500'],['12 Aug','Credit card EMI','₹9,450'],['21 Sep','Jewel loan renewal','₹18,450']].map(x=><div className="timeline-row"><b>{x[0]}</b><CheckCircle2/><span>{x[1]}</span><em>{x[2]}</em><button>Mark paid</button></div>)}</section>;return <><section className="hero"><div className="hero-copy"><div className="pill"><Sparkles size={14}/> YOUR PERSONAL PATH</div><h2>Every payment is a vote<br/>for <em>your freedom.</em></h2><p>Your current plan makes you debt-free <b>14 months sooner</b> than paying the minimums.</p><button>View my freedom plan <ChevronRight size={17}/></button></div><div className="hero-orbit"><div className="orbit-value"><small>LOAN-FREE IN</small><b>5y 2m</b><span>Sep 2031</span></div></div></section><section className="metrics"><Metric label="TOTAL OUTSTANDING" value="₹35,58,000" icon={<Wallet/>}/><Metric label="MONTHLY COMMITMENT" value="₹51,410" icon={<CheckCircle2/>}/><Metric label="INTEREST YOU’LL AVOID" value="₹4,28,600" icon={<TrendingDown/>}/></section><section className="card loans"><p className="kicker">LOAN SNAPSHOT</p><h3>Know every commitment</h3><LoanRows/></section></>}
function Metric({label,value,icon}:{label:string;value:string;icon:React.ReactNode}){return <div className="metric"><div className="metric-icon">{icon}</div><p>{label}</p><b>{value}</b><span>On your plan</span></div>};function LoanRows(){return <div className="loan-list">{loans.map(l=><div className="loan-row"><div className="loan-icon" style={{background:l.color}}><Icon type={l.icon}/></div><div className="loan-name"><b>{l.name}</b><span>{l.emi?`${money(l.emi)} / month`:'Interest-only renewal'}</span></div><div className="loan-balance"><b>{money(l.balance)}</b><span>{l.paid} of {l.total} payments</span></div><div className="progress"><span><i style={{width:`${l.paid/l.total*100}%`,background:l.color}}/></span></div></div>)}</div>}
function FeatureHub({page}:{page:string}){const [extra,setExtra]=useState(10000);const saved=Math.round(extra/9500*1.7);if(page==='Calendar')return <section className="feature-panel"><p className="kicker">AUTOMATIC EMI CALENDAR</p><h2>Never be surprised by a due date.</h2>{[['05 Aug','Home loan EMI','₹29,460'],['08 Aug','Vehicle loan EMI','₹12,500'],['12 Aug','Credit card EMI','₹9,450'],['21 Sep','Jewel loan renewal','₹18,450']].map(x=><div className="feature-row"><b>{x[0]}</b><CircleDollarSign/><span>{x[1]}</span><em>{x[2]}</em><button>Mark paid</button></div>)}</section>;if(page==='Calculator')return <section className="feature-panel two-col"><div><p className="kicker">LOAN CALCULATOR</p><h2>See where every EMI goes.</h2><div className="calc-stat"><span>Home loan EMI</span><b>₹29,460</b><small>₹20,004 principal · ₹9,456 interest</small></div><div className="calc-stat"><span>Remaining balance</span><b>₹28,40,000</b><small>142 payments remaining</small></div></div><div className="donut-card"><b>32%</b><span>interest portion<br/>this month</span></div></section>;if(page==='Strategies')return <section className="feature-panel"><p className="kicker">SNOWBALL VS AVALANCHE</p><h2>Choose the plan that keeps you moving.</h2><div className="compare-grid"><article><span>AVALANCHE · RECOMMENDED</span><b>₹4.28L</b><p>Interest saved by closing the highest-rate loan first.</p><strong>Debt-free in 5y 2m</strong></article><article><span>SNOWBALL</span><b>2 quick wins</b><p>Close smaller balances first for faster motivation.</p><strong>Debt-free in 5y 5m</strong></article></div></section>;if(page==='Cash flow')return <section className="feature-panel"><p className="kicker">CASH FLOW & AFFORDABILITY</p><h2>Know what is safe to pay extra.</h2><div className="cash-grid"><div><span>Monthly income</span><b>₹1,80,000</b></div><div><span>Essential expenses</span><b>₹76,000</b></div><div><span>EMI commitment</span><b>₹51,410</b></div><div className="safe"><span>Safe extra payment</span><b>₹12,590</b></div></div></section>;if(page==='Cascade')return <section className="feature-panel"><p className="kicker">LOAN CLOSURE CASCADE</p><h2>Every closed loan speeds up the next.</h2><div className="cascade"><div><b>May ’27</b><span>Credit card closes</span><em>₹9,450 → Vehicle loan</em></div><div><b>Nov ’28</b><span>Vehicle loan closes</span><em>₹21,950 → Home loan</em></div><div><b>Sep ’31</b><span>Debt-free</span><em>Celebrate your freedom</em></div></div></section>;if(page==='Safeguards')return <section className="feature-panel"><p className="kicker">RENEWAL SAFEGUARDS</p><h2>Your special loans get special care.</h2><div className="alert-card"><ShieldCheck/><div><b>Jewel loan renewal in 61 days</b><span>Set aside ₹18,450 annual interest before Sep 21.</span></div><button>Set reminder</button></div><div className="alert-card"><CircleDollarSign/><div><b>Overdraft interest is due monthly</b><span>Never let an interest-only payment slip through.</span></div><button>Set reminder</button></div></section>;if(page==='Stress score')return <section className="feature-panel stress"><p className="kicker">FINANCIAL STRESS SCORE</p><h2>Clear picture. Calmer choices.</h2><div className="stress-score"><b>72</b><span>Healthy trajectory<br/>with room to improve</span></div><div className="stress-pill">✓ EMIs within income range</div><div className="stress-pill">! Build a 3-month emergency buffer</div></section>;if(page==='Review')return <section className="feature-panel"><p className="kicker">YOUR MONTHLY REVIEW</p><h2>July: progress you can feel.</h2><div className="cash-grid"><div><span>Debt reduced</span><b>₹38,210</b></div><div><span>EMIs paid</span><b>3 / 3</b></div><div><span>Interest avoided</span><b>₹4,860</b></div><div className="safe"><span>Next move</span><b>Credit card EMI</b></div></div></section>;return <><section className="hero"><div className="hero-copy"><div className="pill"><Sparkles size={14}/> YOUR PERSONAL PATH</div><h2>Every payment is a vote<br/>for <em>your freedom.</em></h2><p>Try a bonus or extra EMI and instantly see how much sooner you can be debt-free.</p></div><div className="hero-orbit"><div className="orbit-value"><small>LOAN-FREE IN</small><b>5y 2m</b><span>Sep 2031</span></div></div></section><section className="feature-panel simulator"><p className="kicker">WHAT-IF SIMULATOR</p><h2>What if you pay a little extra?</h2><div className="slider-line"><input type="range" min="0" max="100000" step="1000" value={extra} onChange={e=>setExtra(Number(e.target.value))}/><b>₹{extra.toLocaleString('en-IN')}</b></div><div className="sim-result"><b>{saved} months sooner</b><span>and approximately ₹{(saved*12000).toLocaleString('en-IN')} less interest</span></div></section></>}
function FeatureHub2({page}:{page:string}){if(page==='Documents')return <section className="feature-panel"><p className="kicker">SECURE DOCUMENT VAULT</p><h2>Keep your loan papers together.</h2><p className="sub">Upload sanction letters, statements, payment receipts, or renewal records. PDF, JPG and PNG are supported up to 10 MB.</p><div className="upload-zone"><LockKeyhole size={27}/><b>Drop a document here</b><span>or choose a file from your device</span><input type="file" accept=".pdf,image/png,image/jpeg"/></div></section>;if(page==='Export')return <section className="feature-panel"><p className="kicker">EXPORT & BACKUP</p><h2>Your financial data stays yours.</h2><div className="compare-grid"><article><span>LOANS & PAYMENTS</span><b>CSV export</b><p>Open it in Excel or Google Sheets.</p><a className="primary-cta" href={`${API_URL}/api/exports/loans.csv`}>Download loans</a></article><article><span>COMPLETE BACKUP</span><b>JSON archive</b><p>Keep a portable local copy of your financial history.</p><a className="primary-cta" href={`${API_URL}/api/backups/latest.json`}>Download backup</a></article></div></section>;if(page==='Family')return <section className="feature-panel"><p className="kicker">SHARED FAMILY PLANNING</p><h2>Bring a trusted person into the plan.</h2><p className="sub">Invite a spouse or family member as a viewer or editor. You stay in control and can revoke access at any time.</p><div className="family-form"><input type="email" placeholder="trusted.person@example.com"/><select><option>Viewer — can see the plan</option><option>Editor — can help manage it</option></select><button className="primary-cta">Send invitation <ArrowRight size={16}/></button></div></section>;return <FeatureHub page={page}/>}
function CashEditor(){const u=JSON.parse(localStorage.getItem('nivara_user')||'{}') as User;const [form,setForm]=useState({monthlyIncome:180000,essentialExpenses:76000,monthlySavings:40000,emergencyFund:0,incomeFrequency:'MONTHLY',bonusAmount:0,bonusFrequency:'NONE'});const [editing,setEditing]=useState(false);const [saved,setSaved]=useState(false);const set=(key:string,value:string|number)=>setForm({...form,[key]:value});const save=async()=>{await apiFetch(`/api/users/${u.id}/financial-profile`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setSaved(true);setEditing(false)};return <section className="feature-panel editor-panel"><div className="edit-head"><div><p className="kicker">CASH FLOW PROFILE</p><h2>Make your plan fit real life.</h2></div>{editing?<div className="edit-actions"><button onClick={()=>setEditing(false)}>Cancel</button><button className="primary-cta" onClick={save}>Save changes <CheckCircle2 size={16}/></button></div>:<button className="primary-cta" onClick={()=>setEditing(true)}>Edit profile <ArrowRight size={16}/></button>}</div><div className={'edit-grid '+(!editing?'read-only':'')}>{[['Monthly income','monthlyIncome'],['Essential expenses','essentialExpenses'],['Monthly savings','monthlySavings'],['Emergency fund','emergencyFund'],['Bonus / incentive','bonusAmount']].map(([label,key])=><label>{label}<div><span>₹</span><input disabled={!editing} type="number" value={form[key as keyof typeof form] as number} onChange={e=>set(key,Number(e.target.value))}/></div></label>)}<label>Income frequency<select disabled={!editing} value={form.incomeFrequency} onChange={e=>set('incomeFrequency',e.target.value)}><option>MONTHLY</option><option>WEEKLY</option><option>BIWEEKLY</option><option>YEARLY</option></select></label><label>Bonus frequency<select disabled={!editing} value={form.bonusFrequency} onChange={e=>set('bonusFrequency',e.target.value)}><option>NONE</option><option>ONE_TIME</option><option>MONTHLY</option><option>QUARTERLY</option><option>YEARLY</option></select></label></div>{saved&&<p className="save-note">✓ Cash flow profile saved. Your affordability and stress score will use these values.</p>}</section>}
function CalendarLive(){const [events,setEvents]=useState<any[]>([]);const [loading,setLoading]=useState(true);const load=()=>apiFetch('/api/calendar?months=3').then(r=>r.json()).then(x=>setEvents(x.events||[])).finally(()=>setLoading(false));useEffect(()=>{load()},[]);const paid=async(e:any)=>{await apiFetch(`/api/loans/${e.loanId}/payments`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({amount:e.amount,date:e.date,kind:e.kind})});load()};const unpaid=async(e:any)=>{if(!window.confirm('Mark this payment as unpaid? The loan balance will be restored.'))return;await apiFetch(`/api/loans/${e.loanId}/payments/${e.paymentId}`,{method:'DELETE'});load()};return <section className="feature-panel"><p className="kicker">AUTOMATIC EMI CALENDAR</p><h2>Never be surprised by a due date.</h2>{loading?<p>Loading your schedule…</p>:events.length?events.map(e=><div className="feature-row"><b>{new Date(e.date).toLocaleDateString('en-IN',{day:'2-digit',month:'short'})}</b><CircleDollarSign/><span>{e.loanName} · {e.kind==='EMI'?'EMI':'Interest renewal'}</span><em>{money(e.amount)}</em>{e.status==='paid'?<button onClick={()=>unpaid(e)}>Mark unpaid</button>:<button onClick={()=>paid(e)}>Mark paid</button>}</div>):<div className="upload-zone"><CircleDollarSign size={27}/><b>Your calendar is ready for real loans.</b><span>Add a loan to generate due dates, reminders, and payment actions.</span></div>}</section>}
function CleanHub({page}:{page:string}){const copy:Record<string,[string,string]>={Overview:['Your plan starts with one loan.','Add your first loan to unlock payoff planning, reminders, and your freedom date.'],Calculator:['No loan calculations yet.','Add a loan to see the EMI, principal, interest, and amortization schedule.'],Strategies:['No payoff strategy yet.','Add at least two loans to compare Snowball and Avalanche strategies.'],Cascade:['No closure timeline yet.','Your EMI cascade appears after you add loans and payment terms.'],Safeguards:['No renewal risks right now.','Add jewel or overdraft loans to receive renewal and interest alerts.'],'Stress score':['Your score needs your numbers.','Complete Cash Flow and add loans to calculate a personal financial stress score.'],Review:['No monthly review yet.','Record payments to create your first monthly progress review.'],Documents:['Your document vault is empty.','Upload loan letters, statements, receipts, or renewal records when you are ready.'],Export:['Nothing to export yet.','Your CSV and backup exports will become available after you add loans or payments.'],Family:['Share when your plan is ready.','Invite a trusted family member after you have added your own financial information.']};const [title,desc]=copy[page]||['Welcome to Nivara.','Begin with your real financial information.'];const add=async()=>{const name=window.prompt('Loan name (for example: Home loan)');if(!name)return;const principal=Number(window.prompt('Principal amount'));const rate=Number(window.prompt('Annual interest rate (%)'));const termMonths=Number(window.prompt('Term in months'));const emi=Number(window.prompt('Monthly EMI (enter 0 for interest-only loan)'));if(!principal||!termMonths)return;await apiFetch('/api/loans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,kind:'PERSONAL',principal,outstanding:principal,interestRate:rate,termMonths,emi,startDate:new Date().toISOString(),paymentDueDay:5})});window.location.reload()};return <section className="feature-panel clean-empty"><Sparkles/><p className="kicker">CLEAN START</p><h2>{title}</h2><p>{desc}</p><button className="primary-cta" onClick={add}><Plus size={16}/> Add your first loan</button></section>}
function LoanAwareDashboard({user,logout}:{user:User;logout:()=>void}){const [loans,setLoans]=useState<any[]>([]);const [show,setShow]=useState(false);const load=()=>apiFetch('/api/loans').then(r=>r.json()).then(x=>setLoans(x));useEffect(()=>{load()},[]);return <div className="app-shell"><aside><div className="brand"><img src="/nivara-mark.png"/><span>Nivara</span></div><div className="nav"><button className="active"><Target/>Overview</button><button><CircleDollarSign/>Calendar</button><button><TrendingDown/>Strategies</button><button><Wallet/>Cash flow</button></div><div className="side-bottom"><div className="profile"><div>{user.name.slice(0,2).toUpperCase()}</div><span>{user.name}<br/><small>{loans.length?'Plan active':'New plan'}</small></span><button onClick={logout}><ChevronRight size={16}/></button></div></div></aside><main><header><div><p className="eyebrow">NIVARA FINANCIAL COMMAND CENTER</p><h1>Good morning, {user.name.split(' ')[0]} <span>✦</span></h1><p className="sub">{loans.length?'Your real loans are powering this plan.':'Start with your real numbers—nothing pre-filled.'}</p></div><button className="add-btn" onClick={()=>setShow(true)}><Plus size={18}/> Add a loan</button></header>{loans.length?<section className="feature-panel"><p className="kicker">YOUR ACTIVE LOANS</p><h2>{loans.length} commitment{loans.length>1?'s':''}, one clear plan.</h2><div className="loan-list">{loans.map(l=><div className="loan-row"><div className="loan-icon" style={{background:'#8065ed'}}><Landmark/></div><div className="loan-name"><b>{l.name}</b><span>{l.kind.replace('_',' ')} · {l.interestRate}%</span></div><div className="loan-balance"><b>{money(l.outstanding)}</b><span>{l.emi?`${money(l.emi)} / month`:'Interest-only renewal'}</span></div><div className="progress"><span><i style={{width:'8%',background:'#8065ed'}}/></span><small>{l.termMonths} month term</small></div></div>)}</div></section>:<section className="feature-panel clean-empty"><Sparkles/><p className="kicker">CLEAN START</p><h2>Your plan starts with one loan.</h2><p>Add your real loan details to create calendar events, payoff calculations, and reminders.</p><button className="primary-cta" onClick={()=>setShow(true)}><Plus size={16}/> Add your first loan</button></section>}<AnimatePresence>{show&&<LoanForm close={()=>setShow(false)} saved={()=>{setShow(false);load()}}/>}</AnimatePresence></main></div>}
function LoanForm({close,saved}:{close:()=>void;saved:()=>void}){const [f,setF]=useState({name:'',kind:'PERSONAL',principal:'',interestRate:'',termMonths:'',emi:'',paymentDueDay:'5'});const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');const r=await apiFetch('/api/loans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,principal:Number(f.principal),outstanding:Number(f.principal),interestRate:Number(f.interestRate),termMonths:Number(f.termMonths),emi:Number(f.emi||0),paymentDueDay:Number(f.paymentDueDay),startDate:new Date().toISOString()})});if(!r.ok){setError((await r.json()).message||'Please check your loan details.');return}saved()};const set=(k:string,v:string)=>setF({...f,[k]:v});return <motion.div className="auth-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="auth-modal loan-form" initial={{y:25}} animate={{y:0}}><button className="auth-close" onClick={close}><X/></button><p className="kicker">ADD A REAL LOAN</p><h2>Bring this commitment into focus.</h2><form onSubmit={submit}><label>Loan name<input required value={f.name} onChange={e=>set('name',e.target.value)} placeholder="LIC Home Loan"/></label><label>Loan type<select value={f.kind} onChange={e=>set('kind',e.target.value)}>{['PERSONAL','CREDIT_CARD','HOME','JEWEL','VEHICLE','OVERDRAFT'].map(x=><option value={x}>{x.replace('_',' ')}</option>)}</select></label><div className="two-inputs"><label>Principal / balance<input required type="number" value={f.principal} onChange={e=>set('principal',e.target.value)}/></label><label>Interest rate %<input required type="number" step=".01" value={f.interestRate} onChange={e=>set('interestRate',e.target.value)}/></label></div><div className="two-inputs"><label>Term (months)<input required type="number" value={f.termMonths} onChange={e=>set('termMonths',e.target.value)}/></label><label>Monthly EMI<input type="number" value={f.emi} onChange={e=>set('emi',e.target.value)}/></label></div><label>Payment due day<input required type="number" min="1" max="28" value={f.paymentDueDay} onChange={e=>set('paymentDueDay',e.target.value)}/></label>{error&&<p className="auth-error">{error}</p>}<button className="auth-submit">Save loan <ArrowRight size={16}/></button></form></motion.div></motion.div>}
function FunctionalDashboard({user,logout}:{user:User;logout:()=>void}){const [page,setPage]=useState('Overview');const [loans,setLoans]=useState<any[]>([]);const [show,setShow]=useState(false);const load=()=>apiFetch('/api/loans').then(r=>r.json()).then(setLoans);useEffect(()=>{load()},[]);const nav=['Overview','Calendar','Calculator','Strategies','Cash flow','Cascade','Safeguards','Stress score','Review','Documents','Export','Family'];const body=page==='Calendar'?<CalendarLive/>:page==='Cash flow'?<CashEditor/>:page==='Strategies'?<StrategyLive/>:page==='Overview'?<section className="feature-panel"><p className="kicker">YOUR ACTIVE LOANS</p><h2>{loans.length?`${loans.length} active loan${loans.length>1?'s':''}`:'Start your debt-free plan.'}</h2>{loans.map(l=><div className="loan-row"><div className="loan-icon" style={{background:'#8065ed'}}><Landmark/></div><div className="loan-name"><b>{l.name}</b><span>{l.kind} · {l.interestRate}%</span></div><div className="loan-balance"><b>{money(l.outstanding)}</b><span>{money(l.emi)} / month</span></div></div>)}{!loans.length&&<button className="primary-cta" onClick={()=>setShow(true)}><Plus size={16}/> Add your first loan</button>}</section>:<section className="feature-panel clean-empty"><Sparkles/><p className="kicker">{page.toUpperCase()}</p><h2>{loans.length?'This feature is ready for your loan data.':'Add a loan to unlock this feature.'}</h2><p>{loans.length?'Use the real payments and profile data you enter to personalize this section.':'Nivara never shows fabricated figures for a new account.'}</p></section>;return <div className="app-shell"><aside><div className="brand"><img src="/nivara-mark.png"/><span>Nivara</span></div><div className="nav feature-nav">{nav.map(n=><button className={page===n?'active':''} onClick={()=>setPage(n)}>{n==='Calendar'?<CircleDollarSign/>:n==='Strategies'?<TrendingDown/>:n==='Cash flow'?<Wallet/>:<Target/>}{n}</button>)}</div><div className="side-bottom"><div className="profile"><div>{user.name.slice(0,2)}</div><span>{user.name}</span><button onClick={logout}><ChevronRight size={16}/></button></div></div></aside><main><header><div><p className="eyebrow">NIVARA FINANCIAL COMMAND CENTER</p><h1>{page==='Overview'?`Good morning, ${user.name.split(' ')[0]}`:page}</h1></div><button className="add-btn" onClick={()=>setShow(true)}><Plus size={18}/> Add a loan</button></header>{body}<AnimatePresence>{show&&<LoanForm close={()=>setShow(false)} saved={()=>{setShow(false);load();setPage('Overview')}}/>}</AnimatePresence></main></div>}
function StrategyLive(){const [data,setData]=useState<any>(null);useEffect(()=>{apiFetch('/api/payoff/compare?extraMonthly=0').then(r=>r.json()).then(setData)},[]);return <section className="feature-panel"><p className="kicker">PAYOFF STRATEGIES</p><h2>{data?`${data.recommendation==='avalanche'?'Avalanche':'Snowball'} is recommended.`:'Loading strategies…'}</h2>{data&&<div className="compare-grid"><article><span>AVALANCHE</span><b>{data.avalanche.months} months</b><p>{money(data.avalanche.interestPaid)} projected interest</p></article><article><span>SNOWBALL</span><b>{data.snowball.months} months</b><p>{money(data.snowball.interestPaid)} projected interest</p></article></div>}</section>}
type LivePayment={id:string;amount:number;date:string;kind:'EMI'|'EXTRA'|'INTEREST_RENEWAL'};
type AutomaticProgress={paidEmis:number;remainingEmis:number;principalPaid:number;interestPaid:number;balance:number;recordedEmis:number};
type LiveLoan={id:string;name:string;kind:string;principal:number;outstanding:number;calculatedOutstanding?:number;automaticProgress?:AutomaticProgress;interestRate:number;termMonths:number;emi:number;startDate:string;paymentDueDay:number;payments?:LivePayment[]};
type ScheduleRow={month:number;payment:number;principal:number;interest:number;balance:number};
type Statement={interestOnly:boolean;monthlyInterest?:number;schedule:ScheduleRow[];totals?:{interest:number;principal:number};remainingMonths?:number;totalMonths?:number;completedMonths?:number;fullSchedule?:boolean};

const displayDate=(value:string|Date)=>new Intl.DateTimeFormat('en-IN',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(value));
const dateInput=(value:string|Date)=>{const date=new Date(value);const offset=date.getTimezoneOffset()*60000;return new Date(date.getTime()-offset).toISOString().slice(0,10)};
const addMonths=(date:Date,months:number)=>{const copy=new Date(date);copy.setMonth(copy.getMonth()+months);return copy};
const firstDueDate=(loan:LiveLoan)=>{const first=new Date(loan.startDate);first.setHours(0,0,0,0);return first};

function LoanEditor({loan,close,saved}:{loan?:LiveLoan;close:()=>void;saved:()=>void}){
  const initial={name:loan?.name||'',kind:loan?.kind||'PERSONAL',principal:String(loan?.principal||''),interestRate:String(loan?.interestRate||''),termMonths:String(loan?.termMonths||''),emi:String(loan?.emi||''),paymentDueDay:String(loan?.paymentDueDay||new Date().getDate()),startDate:loan?dateInput(firstDueDate(loan)):dateInput(new Date())};
  const [form,setForm]=useState(initial);const [error,setError]=useState('');const [saving,setSaving]=useState(false);const set=(key:keyof typeof form,value:string)=>setForm(current=>({...current,[key]:value}));
  const term=Math.max(0,Number(form.termMonths)||0);const finalEmiDate=term?addMonths(new Date(`${form.startDate}T12:00:00`),term-1):null;
  const onStartDate=(value:string)=>{const day=new Date(`${value}T12:00:00`).getDate();setForm(current=>({...current,startDate:value,paymentDueDay:String(day)}))};
  const submit=async(event:React.FormEvent)=>{event.preventDefault();setError('');setSaving(true);try{const body={...form,principal:Number(form.principal),interestRate:Number(form.interestRate),termMonths:Number(form.termMonths),emi:Number(form.emi||0),paymentDueDay:Number(form.paymentDueDay),startDate:form.startDate,outstanding:loan?.outstanding??Number(form.principal)};const response=await apiFetch(loan?`/api/loans/${loan.id}`:'/api/loans',{method:loan?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.message||'Please check the loan details.');saved()}catch(err){setError(err instanceof Error?err.message:'Could not save this loan.')}finally{setSaving(false)}};
  return <motion.div className="auth-backdrop loan-editor-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="auth-modal loan-editor-modal" initial={{y:25,opacity:0}} animate={{y:0,opacity:1}} exit={{y:14,opacity:0}}><button className="auth-close" onClick={close} aria-label="Close loan editor"><X/></button><header className="loan-editor-header"><p className="kicker">{loan?'EDIT LOAN':'ADD A REAL LOAN'}</p><h2>{loan?'Keep this loan accurate.':'Bring this commitment into focus.'}</h2><p className="editor-intro">Add the essentials once. Nivara will calculate the final EMI date and repayment journey automatically.</p></header><form className="loan-editor-form" onSubmit={submit}><section className="loan-editor-section"><div className="section-title"><span>01</span><div><b>Loan identity</b><small>How you want to recognize this commitment.</small></div></div><label className="field-full"><span>Loan name</span><input required value={form.name} onChange={event=>set('name',event.target.value)} placeholder="e.g. LIC Home Loan"/></label><label className="field-full"><span>Loan type</span><select value={form.kind} onChange={event=>set('kind',event.target.value)}>{['PERSONAL','CREDIT_CARD','HOME','JEWEL','VEHICLE','OVERDRAFT'].map(kind=><option key={kind} value={kind}>{kind.replace('_',' ')}</option>)}</select></label></section><section className="loan-editor-section"><div className="section-title"><span>02</span><div><b>Loan numbers</b><small>Use the original loan amount and contracted EMI.</small></div></div><div className="form-fields-grid"><label><span>Original principal</span><div className="number-control"><b>₹</b><input required min="1" type="number" value={form.principal} onChange={event=>set('principal',event.target.value)} placeholder="48,00,000"/></div></label><label><span>Interest rate</span><div className="number-control suffix"><input required min="0" type="number" step=".01" value={form.interestRate} onChange={event=>set('interestRate',event.target.value)} placeholder="9.75"/><b>%</b></div></label><label><span>Number of EMIs</span><div className="number-control"><b>#</b><input required min="1" type="number" value={form.termMonths} onChange={event=>set('termMonths',event.target.value)} placeholder="180"/></div></label><label><span>Monthly EMI</span><div className="number-control"><b>₹</b><input min="0" type="number" value={form.emi} onChange={event=>set('emi',event.target.value)} placeholder="50,849"/></div></label></div></section><section className="loan-editor-section schedule-section"><div className="section-title"><span>03</span><div><b>EMI timeline</b><small>Your selected first EMI date drives the full schedule.</small></div></div><div className="form-fields-grid"><label><span>First EMI date</span><input required type="date" value={form.startDate} onChange={event=>onStartDate(event.target.value)}/></label><label><span>EMI due day</span><div className="number-control"><b>Day</b><input required type="number" min="1" max="28" value={form.paymentDueDay} onChange={event=>set('paymentDueDay',event.target.value)}/></div><small className="field-help">Updated automatically when the first EMI date changes.</small></label></div></section><div className="final-emi-preview"><span>PLANNED FINAL EMI</span><b>{finalEmiDate?displayDate(finalEmiDate):'Enter the number of EMIs'}</b><small>{term?`${term} instalments beginning ${displayDate(`${form.startDate}T12:00:00`)}`:'The date is calculated automatically.'}</small></div>{error&&<p className="auth-error">{error}</p>}<button disabled={saving} className="auth-submit">{saving?'Saving…':loan?'Save loan changes':'Save loan'} <ArrowRight size={17}/></button></form></motion.div></motion.div>
}

function loanProgress(loan:LiveLoan){
  if(loan.automaticProgress)return loan.automaticProgress;
  let balance=Number(loan.principal); let principalPaid=0; let interestPaid=0;
  const payments=[...(loan.payments||[])].sort((a,b)=>new Date(a.date).getTime()-new Date(b.date).getTime());
  for(const payment of payments){
    if(payment.kind==='INTEREST_RENEWAL'){interestPaid+=Number(payment.amount);continue}
    const interest=payment.kind==='EXTRA'?0:Math.round(balance*Number(loan.interestRate)/1200);
    const paidInterest=Math.min(Number(payment.amount),interest);
    const paidPrincipal=Math.min(balance,Math.max(0,Number(payment.amount)-paidInterest));
    balance=Math.max(0,balance-paidPrincipal); interestPaid+=paidInterest; principalPaid+=paidPrincipal;
  }
  const paidEmis=payments.filter(payment=>payment.kind==='EMI').length;
  return {paidEmis,remainingEmis:Math.max(0,Number(loan.termMonths)-paidEmis),principalPaid:Math.round(principalPaid),interestPaid:Math.round(interestPaid),balance:Math.round(balance)};
}

function EmiBlocks({paid,total}:{paid:number;total:number}){const blocks=Math.min(36,Math.max(12,total));return <div className="emi-blocks" aria-label={`${paid} of ${total} EMIs paid`}>{Array.from({length:blocks},(_,index)=>{const represented=Math.ceil((index+1)*total/blocks);return <i key={index} className={represented<=paid?'paid':represented-blocks/total<=paid?'current':''}/>})}</div>}

function LoanStatement({loan,close}:{loan:LiveLoan;close:()=>void}){
  const [statement,setStatement]=useState<Statement|null>(null);const [error,setError]=useState('');const progress=loanProgress(loan);
  useEffect(()=>{apiFetch(`/api/loans/${loan.id}/amortization`).then(async response=>{if(!response.ok)throw new Error('The repayment schedule could not be loaded.');return response.json()}).then(setStatement).catch(err=>setError(err instanceof Error?err.message:'Could not load schedule.'))},[loan.id]);
  return <motion.div className="statement-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={close}><motion.section className="statement-modal" initial={{y:28,opacity:0}} animate={{y:0,opacity:1}} exit={{y:18,opacity:0}} onMouseDown={event=>event.stopPropagation()}><div className="statement-head"><div><p className="kicker">LOAN STATEMENT</p><h2>{loan.name}</h2><p>{loan.kind.replaceAll('_',' ')} · {loan.interestRate}% annual interest</p></div><button className="statement-close" onClick={close} aria-label="Close statement"><X/></button></div><div className="statement-summary"><div><span>Current principal</span><b>{money(loan.calculatedOutstanding??progress.balance)}</b></div><div><span>Remaining schedule</span><b>{statement?.interestOnly?'Interest-only':`${statement?.remainingMonths??'—'} months`}</b></div><div><span>Future interest</span><b>{statement?.totals?money(statement.totals.interest):'—'}</b></div></div>{error?<p className="auth-error">{error}</p>:!statement?<p className="statement-loading">Preparing your repayment schedule…</p>:statement.interestOnly?<div className="interest-only-note"><CircleDollarSign/><div><b>This is an interest-only loan.</b><span>Set aside {money(statement.monthlyInterest||0)} each month to keep the loan current. The principal remains {money(loan.calculatedOutstanding??progress.balance)} until you make an extra payment.</span></div></div>:<div className="schedule-wrap"><table><thead><tr><th>#</th><th>Due date</th><th>EMI</th><th>Principal</th><th>Interest</th><th>Balance</th></tr></thead><tbody>{statement.schedule.map(row=><tr key={row.month}><td>{progress.paidEmis+row.month}</td><td>{displayDate(addMonths(firstDueDate(loan),progress.paidEmis+row.month-1))}</td><td>{money(row.payment)}</td><td className="principal-cell">{money(row.principal)}</td><td>{money(row.interest)}</td><td>{money(row.balance)}</td></tr>)}</tbody></table></div>}<div className="statement-footer"><span>Calculated from your actual recorded payments. Extra payments reduce principal first.</span><button className="secondary-action" onClick={()=>window.print()}>Print statement</button></div></motion.section></motion.div>
}

function LoanDetail({loan,openCalendar,onEdit}:{loan:LiveLoan;openCalendar:()=>void;onEdit?:()=>void}){
  const [showStatement,setShowStatement]=useState(false);const progress=loanProgress(loan);const total=Math.max(1,Number(loan.termMonths));const percent=Math.min(100,Math.round(progress.paidEmis/total*100));const emiStart=firstDueDate(loan);const endDate=addMonths(emiStart,Math.max(0,Number(loan.termMonths)-1));
  return <motion.article className="loan-detail" initial={{opacity:0,y:12}} animate={{y:0,opacity:1}}><div className="loan-detail-top"><div className="loan-identity"><div className="loan-icon detail-icon"><Landmark/></div><div><p className="loan-kind">{loan.kind.replaceAll('_',' ')} · {loan.interestRate}%</p><h3>{loan.name}</h3><span>Principal outstanding {money(loan.calculatedOutstanding??progress.balance)}</span></div></div><div className="loan-detail-actions"><button className="secondary-action" onClick={onEdit}>Edit loan</button><button className="secondary-action" onClick={openCalendar}>Payment calendar</button><button className="primary-cta schedule-action" onClick={()=>setShowStatement(true)}>View statement <ArrowRight size={16}/></button></div></div><div className="loan-kpis outstanding-kpis"><div className="outstanding-kpi"><span>Principal outstanding</span><b>{money(loan.calculatedOutstanding??progress.balance)}</b></div><div><span>EMIs paid</span><b>{progress.paidEmis}<small> / {loan.termMonths}</small></b></div><div><span>EMIs remaining</span><b>{progress.remainingEmis}</b></div><div><span>Principal repaid</span><b>{money(progress.principalPaid)}</b></div><div><span>Interest paid</span><b>{money(progress.interestPaid)}</b></div></div><div className="emi-progress"><div className="emi-progress-label"><span><b>{percent}%</b> of EMI journey completed</span><span>{loan.emi?`${money(loan.emi)} each month`:'Interest-only renewal'}</span></div><EmiBlocks paid={progress.paidEmis} total={total}/><div className="emi-key"><span><i className="key-paid"/> Paid</span><span><i className="key-current"/> Next EMI</span><span><i/> Remaining</span></div></div><div className="loan-dates"><div><span>First EMI</span><b>{displayDate(emiStart)}</b></div><div><span>Final EMI</span><b>{displayDate(endDate)}</b></div><div><span>Payment day</span><b>{loan.paymentDueDay} of each month</b></div><div><span>Original principal</span><b>{money(loan.principal)}</b></div></div><AnimatePresence>{showStatement&&<LoanStatement loan={loan} close={()=>setShowStatement(false)}/>}</AnimatePresence></motion.article>
}

function LiveRepaymentView({loans,close}:{loans:LiveLoan[];close:()=>void}){
  const [schedules,setSchedules]=useState<Record<string,Statement>>({});const [loading,setLoading]=useState(true);const [error,setError]=useState('');
  const totalPrincipal=loans.reduce((sum,loan)=>sum+(loan.calculatedOutstanding??loanProgress(loan).balance),0);const totalEmi=loans.reduce((sum,loan)=>sum+(loan.emi||0),0);
  useEffect(()=>{let active=true;setLoading(true);Promise.all(loans.map(async loan=>{const response=await apiFetch(`/api/loans/${loan.id}/amortization?full=true`);if(!response.ok)throw new Error('Could not load the complete repayment schedule.');return [loan.id,await response.json()] as const})).then(entries=>{if(active)setSchedules(Object.fromEntries(entries))}).catch(err=>{if(active)setError(err instanceof Error?err.message:'Could not load schedules.')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[loans]);
  return <motion.div className="statement-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={close}><motion.section className="live-modal" initial={{y:28,opacity:0}} animate={{y:0,opacity:1}} exit={{y:18,opacity:0}} onMouseDown={event=>event.stopPropagation()}><div className="statement-head"><div><p className="kicker">LIVE REPAYMENT VIEW</p><h2>Your plan, as of today.</h2><p>Every loan below includes its complete original repayment schedule.</p></div><button className="statement-close" onClick={close} aria-label="Close live repayment view"><X/></button></div><div className="live-totals"><div><span>Principal outstanding</span><b>{money(totalPrincipal)}</b></div><div><span>Monthly EMI total</span><b>{money(totalEmi)}</b></div><div><span>Active loans</span><b>{loans.length}</b></div></div><div className="live-loan-list">{loans.map(loan=>{const progress=loanProgress(loan);const next=progress.remainingEmis?addMonths(firstDueDate(loan),progress.paidEmis):null;const percent=Math.round(progress.paidEmis/Math.max(1,loan.termMonths)*100);return <article key={loan.id} className="live-loan-row"><div><p>{loan.kind.replaceAll('_',' ')} · {loan.interestRate}%</p><h3>{loan.name}</h3><span>{progress.paidEmis} of {loan.termMonths} EMIs completed · {percent}% of journey</span></div><div><span>Principal repaid</span><b>{money(progress.principalPaid)}</b></div><div><span>Interest paid</span><b>{money(progress.interestPaid)}</b></div><div><span>{next?'Next EMI':'Status'}</span><b>{next?displayDate(next):'Completed'}</b></div></article>})}</div><section className="complete-schedules"><div><p className="kicker">COMPLETE AMORTIZATION SCHEDULE</p><h3>Every EMI, from the first month to the final month.</h3></div>{error?<p className="auth-error">{error}</p>:loading?<p className="statement-loading">Building complete repayment schedules…</p>:loans.map(loan=>{const statement=schedules[loan.id];const progress=loanProgress(loan);return <details key={loan.id} className="full-schedule" open={loans.length===1}><summary><span><b>{loan.name}</b><small>{statement?.schedule.length||loan.termMonths} monthly rows · {money(loan.principal)} original principal</small></span><ArrowRight size={17}/></summary>{statement?.interestOnly?<p className="interest-only-message">Interest-only loan: {money(statement.monthlyInterest||0)} interest is due each month; principal remains outstanding until you make an extra payment.</p>:<div className="schedule-wrap full-schedule-table"><table><thead><tr><th>EMI #</th><th>Due date</th><th>EMI</th><th>Principal</th><th>Interest</th><th>Principal outstanding</th><th>Status</th></tr></thead><tbody>{statement?.schedule.map(row=>{const completed=row.month<=progress.paidEmis;return <tr key={row.month} className={completed?'schedule-completed':''}><td>{row.month}</td><td>{displayDate(addMonths(firstDueDate(loan),row.month-1))}</td><td>{money(row.payment)}</td><td className="principal-cell">{money(row.principal)}</td><td>{money(row.interest)}</td><td>{money(row.balance)}</td><td><span className={completed?'schedule-status completed':'schedule-status upcoming'}>{completed?'Completed':'Upcoming'}</span></td></tr>})}</tbody></table></div>}</details>})}</section><div className="live-footnote"><Sparkles size={16}/><span>Completed months are calculated from the first EMI date through today. The schedule is recalculated whenever the loan details change.</span></div></motion.section></motion.div>
}

type CashFlowForm={monthlyIncome:number;essentialExpenses:number;monthlySavings:number;emergencyFund:number;incomeFrequency:'WEEKLY'|'BIWEEKLY'|'MONTHLY'|'YEARLY';bonusAmount:number;bonusFrequency:'NONE'|'ONE_TIME'|'MONTHLY'|'QUARTERLY'|'YEARLY'};
const emptyCashFlow:CashFlowForm={monthlyIncome:0,essentialExpenses:0,monthlySavings:0,emergencyFund:0,incomeFrequency:'MONTHLY',bonusAmount:0,bonusFrequency:'NONE'};

function CashFlowLive(){
  const user=JSON.parse(localStorage.getItem('nivara_user')||'{}') as User;const [form,setForm]=useState<CashFlowForm>(emptyCashFlow);const [savedForm,setSavedForm]=useState<CashFlowForm>(emptyCashFlow);const [editing,setEditing]=useState(false);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');
  useEffect(()=>{if(!user.id){setLoading(false);setError('Please sign in again to save your Cash Flow profile.');return}let active=true;apiFetch(`/api/users/${user.id}/financial-profile`).then(async response=>{if(!response.ok)throw new Error('Could not load your saved Cash Flow profile.');return response.json()}).then(data=>{if(!active)return;const profile=data.profile as Partial<CashFlowForm>|null;const next={...emptyCashFlow,...(profile||{})};setForm(next);setSavedForm(next)}).catch(err=>{if(active)setError(err instanceof Error?err.message:'Could not load profile.')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[user.id]);
  const set=(key:keyof CashFlowForm,value:string|number)=>{setMessage('');setForm(current=>({...current,[key]:value} as CashFlowForm))};
  const save=async()=>{setSaving(true);setMessage('');setError('');try{const response=await apiFetch(`/api/users/${user.id}/financial-profile`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const profile=await response.json();if(!response.ok)throw new Error(profile.message||'Could not save your Cash Flow profile.');const next={...emptyCashFlow,...profile} as CashFlowForm;setForm(next);setSavedForm(next);setEditing(false);setMessage('Cash Flow profile saved. Your affordability and stress calculations now use these values.')}catch(err){setError(err instanceof Error?err.message:'Could not save profile.')}finally{setSaving(false)}};
  if(loading)return <section className="feature-panel editor-panel"><p className="statement-loading">Loading your saved Cash Flow profile…</p></section>;
  return <section className="feature-panel editor-panel cash-flow-live"><div className="edit-head"><div><p className="kicker">CASH FLOW PROFILE</p><h2>Make your plan fit real life.</h2><p className="cash-flow-note">{editing?'Review your entries, then save them to update your plan.':'Your values are protected. Choose Edit profile to make a change.'}</p></div>{editing?<div className="edit-actions"><button className="secondary-action" onClick={()=>{setForm(savedForm);setEditing(false);setError('')}}>Cancel</button><button disabled={saving} className="primary-cta" onClick={save}>{saving?'Saving…':'Save changes'} <CheckCircle2 size={16}/></button></div>:<button className="primary-cta" onClick={()=>{setMessage('');setError('');setEditing(true)}}>Edit profile <ArrowRight size={16}/></button>}</div><div className={'edit-grid '+(!editing?'read-only':'')}><label>Monthly income<div><span>₹</span><input disabled={!editing} min="0" type="number" value={form.monthlyIncome} onChange={event=>set('monthlyIncome',Number(event.target.value))}/></div></label><label>Essential expenses<div><span>₹</span><input disabled={!editing} min="0" type="number" value={form.essentialExpenses} onChange={event=>set('essentialExpenses',Number(event.target.value))}/></div></label><label>Monthly savings<div><span>₹</span><input disabled={!editing} min="0" type="number" value={form.monthlySavings} onChange={event=>set('monthlySavings',Number(event.target.value))}/></div></label><label>Emergency fund<div><span>₹</span><input disabled={!editing} min="0" type="number" value={form.emergencyFund} onChange={event=>set('emergencyFund',Number(event.target.value))}/></div></label><label>Bonus / incentive<div><span>₹</span><input disabled={!editing} min="0" type="number" value={form.bonusAmount} onChange={event=>set('bonusAmount',Number(event.target.value))}/></div></label><label>Income frequency<select disabled={!editing} value={form.incomeFrequency} onChange={event=>set('incomeFrequency',event.target.value)}><option value="MONTHLY">Monthly</option><option value="WEEKLY">Weekly</option><option value="BIWEEKLY">Every two weeks</option><option value="YEARLY">Yearly</option></select></label><label>Bonus frequency<select disabled={!editing} value={form.bonusFrequency} onChange={event=>set('bonusFrequency',event.target.value)}><option value="NONE">No regular bonus</option><option value="ONE_TIME">One-time</option><option value="MONTHLY">Monthly</option><option value="QUARTERLY">Quarterly</option><option value="YEARLY">Yearly</option></select></label></div>{message&&<p className="save-note">✓ {message}</p>}{error&&<p className="auth-error">{error}</p>}</section>
}

function CalculatorLive({loans}:{loans:LiveLoan[]}){
  const [loanId,setLoanId]=useState('');const [statement,setStatement]=useState<Statement|null>(null);const selected=loans.find(loan=>loan.id===loanId);
  useEffect(()=>{if(loans.length&&!loans.some(loan=>loan.id===loanId))setLoanId(loans[0].id)},[loans,loanId]);
  useEffect(()=>{if(!loanId)return;apiFetch(`/api/loans/${loanId}/amortization`).then(response=>response.json()).then(setStatement).catch(()=>setStatement(null))},[loanId]);
  if(!loans.length)return <FeatureEmpty title="Your calculator needs a loan." text="Add a loan to see exactly how each upcoming EMI splits between principal and interest."/>;
  const next=statement?.schedule[0];return <section className="feature-panel feature-page"><p className="kicker">EMI CALCULATOR</p><h2>See where your next EMI goes.</h2><div className="feature-toolbar"><label>Choose a loan<select value={loanId} onChange={event=>setLoanId(event.target.value)}>{loans.map(loan=><option key={loan.id} value={loan.id}>{loan.name}</option>)}</select></label></div>{selected&&<div className="calculation-grid"><article><span>Current principal outstanding</span><b>{money(selected.calculatedOutstanding??loanProgress(selected).balance)}</b></article><article><span>Monthly EMI</span><b>{money(selected.emi)}</b></article><article><span>Next EMI principal</span><b className="positive">{next?money(next.principal):'—'}</b></article><article><span>Next EMI interest</span><b className="warm">{next?money(next.interest):'—'}</b></article></div>}<div className="next-payment-card"><div><p className="kicker">NEXT PAYMENT BREAKDOWN</p><h3>{next?`${money(next.payment)} is due next.`:'Preparing the breakdown…'}</h3><span>{next?`${money(next.principal)} reduces principal; ${money(next.interest)} is interest.`:'The schedule is loading.'}</span></div><div className="payment-split"><i style={{width:`${next&&next.payment?next.principal/next.payment*100:0}%`}}/><i style={{width:`${next&&next.payment?next.interest/next.payment*100:0}%`}}/></div></div></section>
}

function CascadeLive(){
  const [data,setData]=useState<any>(null);const [view,setView]=useState<'timeline'|'summary'>('timeline');useEffect(()=>{apiFetch('/api/payoff/compare?extraMonthly=0').then(response=>response.json()).then(setData).catch(()=>setData(null))},[]);
  if(!data)return <section className="feature-panel feature-page"><p className="statement-loading">Calculating your payoff cascade…</p></section>;
  const plan=data.recommendation==='snowball'?data.snowball:data.avalanche;const projectedMonth=(months:number)=>{const date=new Date();date.setMonth(date.getMonth()+months);return new Intl.DateTimeFormat('en-IN',{month:'short',year:'numeric'}).format(date)};const maxRemaining=Math.max(1,...data.startingLoans.map((loan:any)=>loan.remainingEmis));
  return <section className="feature-panel feature-page cascade-live-page"><div className="cascade-page-heading"><div><p className="kicker">LOAN CLOSURE CASCADE</p><h2>Every closed EMI powers the next loan.</h2><p>Start from today’s principal outstanding. When a loan closes, its EMI is automatically redirected to the best remaining target.</p></div><div className="cascade-view-toggle"><button className={view==='timeline'?'active':''} onClick={()=>setView('timeline')}>Animated timeline</button><button className={view==='summary'?'active':''} onClick={()=>setView('summary')}>Plan summary</button></div></div><div className="cascade-hero cascade-live-hero"><div><span>Recommended approach</span><b>{data.recommendation==='snowball'?'Snowball':'Avalanche'}</b><p>{data.recommendation==='snowball'?'Build momentum by clearing smaller balances first.':'Direct every freed EMI to the highest-interest balance first.'}</p></div><div><span>Debt-free projection</span><b>{plan.months} months</b><p>{projectedMonth(plan.months)} · {money(plan.interestPaid)} projected interest</p></div><div><span>Current EMI budget</span><b>{money(plan.monthlyCommitment)}<small>/ month</small></b><p>Stays constant while its destination changes.</p></div></div><section className="cascade-starting-point"><div><span>STARTING POINT</span><b>Remaining EMIs and current outstanding</b></div><div className="cascade-starting-loans">{data.startingLoans.map((loan:any)=><article key={loan.id}><div className="cascade-loan-top"><b>{loan.name}</b><strong>{loan.remainingEmis} EMI{loan.remainingEmis===1?'':'s'} left</strong></div><div className="cascade-block-track" aria-label={`${loan.remainingEmis} remaining EMIs`}><motion.i initial={{width:0}} animate={{width:`${Math.max(7,loan.remainingEmis/maxRemaining*100)}%`}} transition={{duration:.75,ease:'easeOut'}}/></div><div><span>{money(loan.balance)} outstanding</span><span>{money(loan.emi)} / month</span></div></article>)}</div></section><AnimatePresence mode="wait">{view==='timeline'?<motion.section key="timeline" className="cascade-timeline" initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-8}}>{plan.closures.length?plan.closures.map((closure:any,index:number)=><motion.article key={closure.loanId} className="cascade-timeline-event" initial={{opacity:0,x:-18}} animate={{opacity:1,x:0}} transition={{delay:index*.16,duration:.42}}><div className="cascade-timeline-rail"><motion.span initial={{scale:0}} animate={{scale:1}} transition={{delay:index*.16+.1,type:'spring'}}>{String(index+1).padStart(2,'0')}</motion.span>{index<plan.closures.length-1&&<i/>}</div><div className="cascade-timeline-card"><div className="cascade-event-meta"><span>MONTH {closure.month} · {projectedMonth(closure.month)}</span><strong>{closure.name} closes</strong></div><p>{money(closure.freedEmi)} each month is released from this loan.</p><div className="cascade-money-flow"><motion.div className="cascade-fund-chip" initial={{scale:.8,opacity:0}} animate={{scale:1,opacity:1}} transition={{delay:index*.16+.2}}>{money(closure.freedEmi)}/mo</motion.div><div className="cascade-flow-line"><i/></div><div className="cascade-flow-target"><span>{closure.redirectedTo==='Your debt-free finish'?'Final milestone':'Redirected to'}</span><b>{closure.redirectedTo}</b></div></div><div className="cascade-power-note"><Sparkles size={16}/><span>Monthly payoff power becomes <b>{money(closure.monthlyPower)}</b>.</span></div></div></motion.article>):<div className="review-no-schedule"><CircleDollarSign/><div><b>Your cascade is waiting for a loan with an EMI.</b><span>Add repayment terms to map how the funds will flow.</span></div></div>}{plan.unresolvedLoans?.length>0&&<p className="cascade-warning">Some interest-only balances need an extra payment plan to reach a closure date: {plan.unresolvedLoans.join(', ')}.</p>}</motion.section>:<motion.section key="summary" className="cascade-summary-view" initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-8}}>{plan.closures.map((closure:any,index:number)=><article key={closure.loanId}><span>{String(index+1).padStart(2,'0')}</span><div><small>Expected closure · {projectedMonth(closure.month)}</small><b>{closure.name}</b><p>Redirect {money(closure.freedEmi)}/month to {closure.redirectedTo}.</p></div><strong>{money(closure.monthlyPower)}/mo power</strong></article>)}</motion.section>}</AnimatePresence></section>
}

function SafeguardsLive(){
  const [alerts,setAlerts]=useState<any[]|null>(null);useEffect(()=>{apiFetch('/api/safeguards').then(response=>response.json()).then(data=>setAlerts(data.alerts||[])).catch(()=>setAlerts([]))},[]);
  return <section className="feature-panel feature-page"><p className="kicker">RENEWAL SAFEGUARDS</p><h2>Stay ahead of special-loan deadlines.</h2>{alerts===null?<p className="statement-loading">Checking your renewal deadlines…</p>:alerts.length?<div className="safeguard-list">{alerts.map(alert=><article key={alert.loanId} className={alert.severity}><ShieldCheck/><div><b>{alert.loanName}</b><span>{alert.action} · {displayDate(alert.dueDate)}</span></div><strong>{money(alert.amount)}</strong><small>{alert.daysRemaining} days left</small></article>)}</div>:<div className="feature-success"><ShieldCheck/><div><b>No special-loan deadlines right now.</b><span>Add Jewel or Overdraft loans to receive renewal and interest safeguards here.</span></div></div>}</section>
}

function StressScoreLive(){
  const user=JSON.parse(localStorage.getItem('nivara_user')||'{}') as User;const [data,setData]=useState<any>(null);useEffect(()=>{if(user.id)apiFetch(`/api/users/${user.id}/stress-score`).then(response=>response.json()).then(setData).catch(()=>setData({configured:false}))},[user.id]);
  if(!data)return <section className="feature-panel feature-page"><p className="statement-loading">Calculating your personal stress score…</p></section>;
  if(!data.configured)return <FeatureEmpty title="Your score needs your Cash Flow profile." text="Add income, expenses, savings, and an emergency fund to calculate a personal financial stress score."/>;
  return <section className="feature-panel feature-page"><p className="kicker">FINANCIAL STRESS SCORE</p><h2>Clear picture. Calmer choices.</h2><div className="stress-live"><div className={`score-orb ${data.level}`}><b>{data.score}</b><span>/ 100</span></div><div><b className="score-level">{data.level}</b><p>Your score reflects EMI pressure, emergency-fund coverage, high-interest loans, and total outstanding balance.</p><div className="stress-metrics"><span>EMI / income <b>{data.metrics.emiToIncomePercent}%</b></span><span>Emergency fund <b>{data.metrics.emergencyFundMonths} months</b></span><span>High-interest loans <b>{data.metrics.highInterestLoans}</b></span></div></div></div><div className="action-list">{data.actions.map((action:string,index:number)=><div key={action}><span>{index+1}</span>{action}</div>)}</div></section>
}

function ReviewLive(){
  const [month,setMonth]=useState(()=>{const now=new Date();return `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}`});const [review,setReview]=useState<any>(null);const [loading,setLoading]=useState(true);
  useEffect(()=>{setLoading(true);apiFetch(`/api/reviews/monthly?month=${encodeURIComponent(month)}`).then(response=>response.ok?response.json():Promise.reject()).then(setReview).catch(()=>setReview(null)).finally(()=>setLoading(false))},[month]);
  if(loading)return <section className="feature-panel feature-page"><p className="statement-loading">Preparing your monthly review…</p></section>;
  if(!review)return <section className="feature-panel feature-page"><p className="statement-loading">Your review could not be loaded. Please refresh and try again.</p></section>;
  return <section className="feature-panel feature-page review-live-page"><div className="review-live-heading"><div><p className="kicker">MONTHLY REVIEW</p><h2>Progress you can feel.</h2></div><label className="review-month-control"><span>Review month</span><input type="month" value={month} onChange={event=>setMonth(event.target.value)}/></label></div><p className="review-message">{review.message}</p><div className="calculation-grid review-calculation-grid"><article><span>Scheduled this month</span><b>{money(review.scheduledAmount)}</b><small>{review.scheduledEmis} planned payment{review.scheduledEmis===1?'':'s'}</small></article><article><span>Counted from plan</span><b>{review.countedEmis} / {review.scheduledEmis}</b><small>Based on first EMI dates</small></article><article><span>Confirmed in calendar</span><b>{review.confirmedEmis}</b><small>Marked paid by you</small></article><article><span>Principal reduction</span><b className="positive">{money(review.debtReduction)}</b><small>{money(review.estimatedInterestPaid)} interest</small></article><article><span>Extra principal</span><b className="positive">{money(review.extraPaid)}</b><small>Additional loan payment</small></article></div><div className="review-schedule-card"><div className="review-schedule-heading"><div><span>MONTHLY PAYMENT PLAN</span><b>Where this month’s EMI goes</b></div><small>Schedule is calculated from each loan’s first EMI date.</small></div>{review.schedule?.length?<div className="review-schedule-list">{review.schedule.map((item:any)=><article key={`${item.loanId}-${item.date}`} className={`review-schedule-row ${item.status}`}><div className="review-schedule-loan"><span>{displayDate(item.date)}</span><b>{item.loanName}</b></div><div><span>Principal</span><b>{money(item.principal)}</b></div><div><span>Interest</span><b>{money(item.interest)}</b></div><div><span>EMI</span><b>{money(item.payment)}</b></div><strong>{item.status==='confirmed'?'Confirmed':item.status==='counted'?'Counted from plan':'Upcoming'}</strong></article>)}</div>:<div className="review-no-schedule"><CircleDollarSign/><div><b>No scheduled payments in this month.</b><span>Choose another month or add a loan with a first EMI date.</span></div></div>}</div><div className="review-next"><Sparkles/><div><span>Next best move</span><b>{review.nextBestMove}</b></div></div></section>
}

function DocumentsLive({loans}:{loans:LiveLoan[]}){
  const user=JSON.parse(localStorage.getItem('nivara_user')||'{}') as User;const [documents,setDocuments]=useState<any[]>([]);const [file,setFile]=useState<File|null>(null);const [loanId,setLoanId]=useState('');const [message,setMessage]=useState('');const [uploading,setUploading]=useState(false);
  const load=()=>{if(user.id)apiFetch(`/api/users/${user.id}/documents`).then(response=>response.json()).then(setDocuments).catch(()=>setDocuments([]))};useEffect(()=>{load()},[user.id]);
  const upload=async()=>{if(!file)return setMessage('Choose a PDF, JPG, or PNG file first.');setUploading(true);setMessage('');try{const body=new FormData();body.append('file',file);if(loanId)body.append('loanId',loanId);const response=await apiFetch(`/api/users/${user.id}/documents`,{method:'POST',body});const data=await response.json();if(!response.ok)throw new Error(data.message||'Upload failed.');setFile(null);setMessage('Document saved securely.');load()}catch(err){setMessage(err instanceof Error?err.message:'Upload failed.')}finally{setUploading(false)}};
  const download=async(document:any)=>{setMessage('');try{const response=await apiFetch(`/api/users/${user.id}/documents/${document.id}/download`);if(!response.ok)throw new Error((await response.json()).message||'Download failed.');if(response.headers.get('content-type')?.includes('application/json')){const data=await response.json();window.open(data.url,'_blank','noopener,noreferrer');return;}const blob=await response.blob();const url=URL.createObjectURL(blob);const anchor=window.document.createElement('a');anchor.href=url;anchor.download=document.name;anchor.click();URL.revokeObjectURL(url)}catch(err){setMessage(err instanceof Error?err.message:'Download failed.')}};
  return <section className="feature-panel feature-page document-vault-page"><p className="kicker">SECURE DOCUMENT VAULT</p><h2>Keep loan papers together.</h2><p className="vault-intro">Store sanction letters, statements, receipts, and renewal records beside the plan they support.</p><div className="document-picker-card"><label className="document-dropzone"><input className="document-file-input" type="file" accept="application/pdf,image/png,image/jpeg" onChange={event=>setFile(event.target.files?.[0]||null)}/><span className="document-drop-icon"><LockKeyhole size={23}/></span><span className="document-drop-copy"><b>{file?file.name:'Choose a document'}</b><small>{file?`${Math.max(1,Math.round(file.size/1024))} KB selected`:'PDF, JPG or PNG · up to 10 MB'}</small></span><span className="document-browse">Browse files</span></label><div className="document-upload-actions"><label>Attach to loan<select value={loanId} onChange={event=>setLoanId(event.target.value)}><option value="">General document</option>{loans.map(loan=><option key={loan.id} value={loan.id}>{loan.name}</option>)}</select></label><button className="primary-cta" disabled={uploading||!file} onClick={upload}>{uploading?'Uploading…':'Upload securely'} <ArrowRight size={16}/></button></div></div>{message&&<p className="document-message">{message}</p>}<div className="document-list">{documents.length?documents.map(document=><article key={document.id}><LockKeyhole/><div><b>{document.name}</b><small>{(document.sizeBytes/1024).toFixed(0)} KB · {displayDate(document.createdAt)}</small></div><button className="document-download" onClick={()=>download(document)}>Download</button></article>):<div className="feature-success"><LockKeyhole/><div><b>Your vault is empty.</b><span>Choose a file above to add your first loan paper securely.</span></div></div>}</div></section>
}

function ExportLive(){const [message,setMessage]=useState('');const download=async(path:string,name:string)=>{setMessage('');try{const response=await apiFetch(path);if(!response.ok)throw new Error((await response.json()).message||'Export failed.');const blob=await response.blob();const url=URL.createObjectURL(blob);const anchor=window.document.createElement('a');anchor.href=url;anchor.download=name;anchor.click();URL.revokeObjectURL(url);setMessage(`${name} downloaded.`)}catch(err){setMessage(err instanceof Error?err.message:'Export failed.')}};return <section className="feature-panel feature-page"><p className="kicker">EXPORT & BACKUP</p><h2>Your financial data stays yours.</h2><div className="export-grid"><button className="export-download" onClick={()=>download('/api/exports/loans.csv','nivara-loans.csv')}><Wallet/><div><b>Loans CSV</b><span>Loan terms, outstanding balances, and payment counts.</span></div><ArrowRight size={18}/></button><button className="export-download" onClick={()=>download('/api/exports/payments.csv','nivara-payments.csv')}><CircleDollarSign/><div><b>Payments CSV</b><span>Every recorded EMI, extra payment, and renewal.</span></div><ArrowRight size={18}/></button><button className="export-download" onClick={()=>download('/api/backups/latest.json','nivara-backup.json')}><LockKeyhole/><div><b>Complete backup</b><span>A portable JSON archive of your Nivara data.</span></div><ArrowRight size={18}/></button></div>{message&&<p className="document-message">{message}</p>}</section>}

function FamilyLive(){
  const user=JSON.parse(localStorage.getItem('nivara_user')||'{}') as User;const [shares,setShares]=useState<any[]>([]);const [email,setEmail]=useState('');const [role,setRole]=useState('VIEWER');const [message,setMessage]=useState('');
  const load=()=>{if(user.id)apiFetch(`/api/users/${user.id}/family-shares`).then(response=>response.json()).then(setShares).catch(()=>setShares([]))};useEffect(()=>{load()},[user.id]);
  const invite=async()=>{if(!email)return setMessage('Enter an email address first.');const response=await apiFetch(`/api/users/${user.id}/family-shares`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,role})});if(!response.ok)return setMessage((await response.json()).message||'Could not send invitation.');setEmail('');setMessage('Invitation created.');load()};
  const revoke=async(id:string)=>{await apiFetch(`/api/users/${user.id}/family-shares/${id}`,{method:'DELETE'});load()};
  return <section className="feature-panel feature-page"><p className="kicker">FAMILY SHARING</p><h2>Bring a trusted person into the plan.</h2><div className="family-live-form"><input type="email" value={email} onChange={event=>setEmail(event.target.value)} placeholder="trusted.person@example.com"/><select value={role} onChange={event=>setRole(event.target.value)}><option value="VIEWER">Viewer — can see the plan</option><option value="EDITOR">Editor — can help manage it</option></select><button className="primary-cta" onClick={invite}>Send invitation <ArrowRight size={16}/></button></div>{message&&<p className="document-message">{message}</p>}<div className="share-list">{shares.length?shares.map(share=><article key={share.id}><HeartHandshake/><div><b>{share.recipient?.name||share.email}</b><span>{share.email} · {share.role.toLowerCase()} · {share.status.toLowerCase()}</span></div><button className="secondary-action" onClick={()=>revoke(share.id)}>Revoke</button></article>):<div className="feature-success"><HeartHandshake/><div><b>No one else can access your plan.</b><span>Invite a trusted viewer or editor whenever you are ready.</span></div></div>}</div></section>
}

function FeatureEmpty({title,text}:{title:string;text:string}){return <section className="feature-panel feature-page clean-empty"><Sparkles/><h2>{title}</h2><p>{text}</p></section>}

function DashboardV3({user,logout}:{user:User;logout:()=>void}){
  const [page,setPage]=useState('Overview');
  const [loans,setLoans]=useState<LiveLoan[]>([]);
  const [showEditor,setShowEditor]=useState(false);
  const [showLiveRepayment,setShowLiveRepayment]=useState(false);
  const [editingLoan,setEditingLoan]=useState<LiveLoan|undefined>(undefined);
  const [loading,setLoading]=useState(true);
  const load=()=>{setLoading(true);apiFetch('/api/loans').then(async response=>{if(!response.ok)throw new Error('Could not load loans.');return response.json()}).then(setLoans).catch(()=>setLoans([])).finally(()=>setLoading(false))};
  useEffect(()=>{load()},[]);
  useEffect(()=>{if(page==='Overview')load()},[page]);
  const openNewLoan=()=>{setEditingLoan(undefined);setShowEditor(true)};
  const openEditor=(loan:LiveLoan)=>{setEditingLoan(loan);setShowEditor(true)};
  const nav=['Overview','Calendar','Calculator','Strategies','Cash flow','Cascade','Safeguards','Stress score','Review','Documents','Export','Family'];
  const overview=<section className="loans-overview"><div className="overview-title"><div><p className="kicker">YOUR ACTIVE LOANS</p><h2>{loans.length?`${loans.length} active loan${loans.length>1?'s':''}, one transparent plan.`:'Your debt-free plan starts here.'}</h2></div>{loans.length>0&&<span className="overview-live">Live repayment view</span>}</div>{loading?<p className="statement-loading">Loading your loans…</p>:loans.length?<div className="loan-detail-list">{loans.map(loan=><LoanDetail key={loan.id} loan={loan} openCalendar={()=>setPage('Calendar')} onEdit={()=>openEditor(loan)}/>)}</div>:<div className="clean-empty"><Sparkles/><h3>Add your first real loan</h3><p>Enter the first EMI date and instalment count to create a clear payment timeline—without sample data.</p><button className="primary-cta" onClick={openNewLoan}><Plus size={16}/> Add your first loan</button></div>}</section>;
  const interactiveOverview=<section className="loans-overview"><div className="overview-title"><div><p className="kicker">YOUR ACTIVE LOANS</p><h2>{loans.length?`${loans.length} active loan${loans.length>1?'s':''}, one transparent plan.`:'Your debt-free plan starts here.'}</h2></div>{loans.length>0&&<button className="overview-live" onClick={()=>setShowLiveRepayment(true)}>Live repayment view <ArrowRight size={14}/></button>}</div>{loading?<p className="statement-loading">Loading your loans…</p>:loans.length?<div className="loan-detail-list">{loans.map(loan=><LoanDetail key={loan.id} loan={loan} openCalendar={()=>setPage('Calendar')} onEdit={()=>openEditor(loan)}/>)}</div>:<div className="clean-empty"><Sparkles/><h3>Add your first real loan</h3><p>Enter the first EMI date and instalment count to create a clear payment timeline—without sample data.</p><button className="primary-cta" onClick={openNewLoan}><Plus size={16}/> Add your first loan</button></div>}<AnimatePresence>{showLiveRepayment&&<LiveRepaymentView loans={loans} close={()=>setShowLiveRepayment(false)}/>}</AnimatePresence></section>;
  const emptyFeature=<section className="feature-panel clean-empty"><Sparkles/><p className="kicker">{page.toUpperCase()}</p><h2>{loans.length?'This area is ready for your real plan.':'Add a loan to unlock this feature.'}</h2><p>{loans.length?'Use the details you enter here to make your repayment plan more useful.':'New accounts stay clean: no made-up balances or sample progress.'}</p></section>;
  const content=page==='Overview'?interactiveOverview:page==='Calendar'?<CalendarLive/>:page==='Calculator'?<CalculatorLive loans={loans}/>:page==='Strategies'?<StrategyLive/>:page==='Cash flow'?<CashFlowLive/>:page==='Cascade'?<CascadeLive/>:page==='Safeguards'?<SafeguardsLive/>:page==='Stress score'?<StressScoreLive/>:page==='Review'?<ReviewLive/>:page==='Documents'?<DocumentsLive loans={loans}/>:page==='Export'?<ExportLive/>:page==='Family'?<FamilyLive/>:emptyFeature;
  return <div className="app-shell"><aside><div className="brand"><img src="/nivara-mark.png"/><span>Nivara</span></div><div className="nav feature-nav">{nav.map(name=><button key={name} className={page===name?'active':''} onClick={()=>setPage(name)}>{name==='Calendar'?<CircleDollarSign/>:name==='Strategies'||name==='Calculator'?<TrendingDown/>:name==='Cash flow'?<Wallet/>:<Target/>}{name}</button>)}</div><div className="side-bottom"><div className="profile"><div>{user.name.slice(0,2).toUpperCase()}</div><span>{user.name}<small>{loans.length?'Plan active':'New plan'}</small></span><button onClick={logout} aria-label="Log out"><ChevronRight size={16}/></button></div></div></aside><main><header><div><p className="eyebrow">NIVARA FINANCIAL COMMAND CENTER</p><h1>{page==='Overview'?`Good morning, ${user.name.split(' ')[0]}`:page} <span>✦</span></h1><p className="sub">{page==='Overview'?'Your payments, principal, and next steps in one calm view.':'Make one clear move at a time.'}</p></div><button className="add-btn" onClick={openNewLoan}><Plus size={18}/> Add a loan</button></header>{content}<AnimatePresence>{showEditor&&<LoanEditor loan={editingLoan} close={()=>setShowEditor(false)} saved={()=>{setShowEditor(false);setEditingLoan(undefined);setPage('Overview');load()}}/>}</AnimatePresence></main></div>
}

function DashboardV2({user,logout}:{user:User;logout:()=>void}){
  const [page,setPage]=useState('Overview');const [loans,setLoans]=useState<LiveLoan[]>([]);const [show,setShow]=useState(false);const [loading,setLoading]=useState(true);
  const load=()=>{setLoading(true);apiFetch('/api/loans').then(async response=>{if(!response.ok)throw new Error();return response.json()}).then(setLoans).catch(()=>setLoans([])).finally(()=>setLoading(false))};
  useEffect(()=>{load()},[]);useEffect(()=>{if(page==='Overview')load()},[page]);
  const nav=['Overview','Calendar','Calculator','Strategies','Cash flow','Cascade','Safeguards','Stress score','Review','Documents','Export','Family'];
  const content=page==='Overview'?<section className="loans-overview"><div className="overview-title"><div><p className="kicker">YOUR ACTIVE LOANS</p><h2>{loans.length?`${loans.length} active loan${loans.length>1?'s':''}, one transparent plan.`:'Your debt-free plan starts here.'}</h2></div>{loans.length>0&&<span className="overview-live">Live repayment view</span>}</div>{loading?<p className="statement-loading">Loading your loans…</p>:loans.length?<div className="loan-detail-list">{loans.map(loan=><LoanDetail key={loan.id} loan={loan} openCalendar={()=>setPage('Calendar')}/>)}</div>:<div className="clean-empty"><Sparkles/><h3>Add your first real loan</h3><p>We’ll turn it into a clear repayment schedule, calendar, and payoff plan—without any sample data.</p><button className="primary-cta" onClick={()=>setShow(true)}><Plus size={16}/> Add your first loan</button></div>}</section>:page==='Calendar'?<CalendarLive/>:page==='Cash flow'?<CashEditor/>:page==='Strategies'?<StrategyLive/>:<section className="feature-panel clean-empty"><Sparkles/><p className="kicker">{page.toUpperCase()}</p><h2>{loans.length?'This area is ready for your real plan.':'Add a loan to unlock this feature.'}</h2><p>{loans.length?'Your figures are kept separate from the dashboard and used only when this feature needs them.':'Nivara keeps a new account clean—no fabricated balances or progress.'}</p></section>;
  return <div className="app-shell"><aside><div className="brand"><img src="/nivara-mark.png"/><span>Nivara</span></div><div className="nav feature-nav">{nav.map(name=><button key={name} className={page===name?'active':''} onClick={()=>setPage(name)}>{name==='Calendar'?<CircleDollarSign/>:name==='Strategies'||name==='Calculator'?<TrendingDown/>:name==='Cash flow'?<Wallet/>:<Target/>}{name}</button>)}</div><div className="side-bottom"><div className="profile"><div>{user.name.slice(0,2).toUpperCase()}</div><span>{user.name}<small>{loans.length?'Plan active':'New plan'}</small></span><button onClick={logout} aria-label="Log out"><ChevronRight size={16}/></button></div></div></aside><main><header><div><p className="eyebrow">NIVARA FINANCIAL COMMAND CENTER</p><h1>{page==='Overview'?`Good morning, ${user.name.split(' ')[0]}`:page} <span>✦</span></h1><p className="sub">{page==='Overview'?'Your payments, principal, and next steps in one calm view.':'Make one clear move at a time.'}</p></div><button className="add-btn" onClick={()=>setShow(true)}><Plus size={18}/> Add a loan</button></header>{content}<AnimatePresence>{show&&<LoanForm close={()=>setShow(false)} saved={()=>{setShow(false);setPage('Overview');load()}}/>}</AnimatePresence></main></div>
}

createRoot(document.getElementById('root')!).render(<App/>);
